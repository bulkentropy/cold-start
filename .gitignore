__pycache__/
*.pyc
_q_*.sql
.env
.env.*

# Carries CSP names against FPV enforcement reasons; this repo is public.
# Served from the private cs-docs bucket at runtime — see _load_august().
data/august_payout.json

# 13 named CSPs with their Sehat cycle-1 payouts; served from cs-docs at runtime.
data/sehat_payout.json

# Enforcement Hub removal list: names, mobiles, emails, FPV reasons.
# Served from cs-docs at runtime - see MG_REMOVED_OBJECT.
data/mg_removed.json
